# Issue 1: no Cascade Delete in Mongoose 
=> Mongoose er default cascade deletion option nai. so we have 3 options: 1. Document middleware (inside model - only works when we call a certain function like deleteOne) 2. Query Middleware (can be made work with findByIdAndDelete if we want) 3. Service layer function (safest but more messy service layer code); I chose to work with document layer middleware. let's see


* MongoDB does not have cascade delete because it is not a relational db so there is no foreign key :)

*** In express, always put static paths above, dynamic paths below.
        router.get("/myposts", authenticate, PostController.getMyPosts);
        router.get("/", PostController.getAll);
        router.get("/:id", PostController.getById);

        this is OK.

        but
        router.get("/", PostController.getAll);
        router.get("/:id", PostController.getById);
        router.get("/myposts", authenticate, PostController.getMyPosts);

        this is not. because now express will treat "myposts" as the id and execute /:id route. this is a critical thing with EXPRESS




# issue 2: Signup from backend does not return a token. only login does. had to suffer because of this. so auto login = signup + login back to back.
THE FIX ? ===> call login in the singup component so that after signup, login automatically is called



*** setState in react is asynchronous and batched. 
setIncoming(inc.data);  // it says "please update "incoming" on the next render"
incoming.find(...);     // so this is still reading THIS render's "incoming"
* faced this issue implementing incoming friend request er jonno friend status



*** Implementing chat
-> Socket.io needs the raw http.Server, not the Express app directly — that's why you wrap it with http.createServer(app).




# issue 3 : render e server deploy korar time e build error khacchilo : src/socket/socket.ts(3,37): error TS2307: Cannot find module 'socket.io' or its corresponding type declarations. 
--> this happened because node_modules e socket.io khuje paynai. karon oita root e install hoy and 


=========================================================================================================================================================================================

### TODO: 

=========================================================================================================================================================================================

1. Reset password pipeline
